# 프로젝트를 통해 배운 핵심 개념 — 뉴스 한 줄 요약봇

> "AI 웹 개발" 과제를 진행하며 실제로 이해하고 설명할 수 있게 된 핵심 개념 11가지를 정리한 문서입니다. 각 항목은 이 프로젝트(`api/summarize.py`, `js/main.js`, `summarize.html` 등)의 실제 코드를 근거로 설명합니다.

---

## 1. 프론트엔드 vs 백엔드 — 어디서 실행되는가

- **프론트엔드(HTML/CSS/JS)**: 사용자의 브라우저에서 실행됨
- **백엔드(Python, `api/summarize.py`)**: 서버(Vercel)에서 실행됨

구분 기준은 파일 확장자가 아니라 **"어느 컴퓨터에서 코드가 돌아가는가"**입니다. 이 프로젝트에서는 `index.html`, `css/style.css`, `js/main.js`가 모두 프론트엔드(브라우저 실행)이고, `api/summarize.py`만 백엔드(서버 실행)입니다.

## 2. 정적 페이지 vs 동적 페이지

| 구분 | 특징 | 이 프로젝트의 예 |
|---|---|---|
| 정적 페이지 | 누가 언제 접속해도 항상 같은 내용, 서버가 별도 계산을 하지 않음 | `index.html`, `guide.html`, `about.html` |
| 동적 페이지 | 입력값에 따라 결과가 매번 새로 계산됨 | `summarize.html` (입력한 URL마다 다른 요약 결과 반환) |

## 3. 프론트-백엔드 통신의 실체 — `fetch`

JavaScript의 `fetch()`는 같은 파일 안에서 함수를 부르는 것과 근본적으로 다릅니다. **인터넷을 통해 서버에게 요청 편지를 보내고, 응답 편지를 기다리는 과정**입니다.

```javascript
const response = await fetch('/api/summarize', {
  method: 'POST',
  body: JSON.stringify({ url: url }),
});
```

네트워크를 거치기 때문에 시간이 걸리고 실패할 수 있습니다. 그래서 `await`(응답을 기다림)와 `try/catch`(실패 시 처리)가 반드시 필요합니다.

## 4. 데이터는 단계마다 형태를 바꾸며 흘러간다

```
url (짧은 주소 문자열)
  → html (페이지 원본 코드, 광고/메뉴/본문 다 섞임)
    → article_text (순수 기사 본문 텍스트만)
      → JSON (AI가 생성한 요약 + 키워드)
```

`api/summarize.py`의 `fetch_html(url)` → `extract_body(html)` → `summarize_article(article_text)` 세 함수는 각각 인자 이름이 다른데, 이는 각 단계를 거치며 데이터의 형태가 완전히 달라지기 때문입니다. 변수명은 "지금 이 데이터가 무엇인지"를 알려주는 역할을 합니다.

## 5. AI(실제 이해/판단)와 일반 알고리즘(규칙 기반 자동화)의 차이

| 함수 | 실제로 하는 일 | AI 여부 |
|---|---|---|
| `fetch_html` | 정해진 절차대로 URL에 접속해 HTML을 받아옴 | ❌ 일반 HTTP 통신 |
| `extract_body` | 미리 정해진 규칙(텍스트 밀도, 태그 구조 등)으로 본문을 판별 | ❌ 규칙 기반 알고리즘(`trafilatura`) |
| `summarize_article` | 텍스트의 의미를 이해하고 새로운 요약 문장을 생성 | ✅ 실제 AI(Claude) |

세 함수 모두 "자동으로 처리"되지만, 실제로 내용을 **이해하고 판단**하는 것은 `summarize_article` 하나뿐입니다.

## 6. 실패 처리의 중요성

정상적으로 동작하는 것과, 예외 상황까지 대비된 것은 완성도가 다릅니다. 이 프로젝트는 아래 6가지 실패 케이스를 설계하고 실제로 테스트했습니다.

| 실패 상황 | 처리 위치 | 안내 메시지 |
|---|---|---|
| 빈 입력 | 프론트 1차 검증 | "뉴스 기사 URL을 입력해주세요." |
| URL 형식 오류 | 프론트 1차 검증 | "올바른 URL 형식이 아니에요." |
| 크롤링 실패 | 백엔드 | "해당 페이지에서 기사를 가져올 수 없어요." |
| 본문 추출 실패 | 백엔드 | "기사 본문을 인식하지 못했어요." |
| AI API 오류 | 백엔드 | "일시적인 오류가 발생했습니다." |
| 응답 지연/타임아웃 | 프론트(`AbortController`) | "요약 생성이 너무 오래 걸려요." |

## 7. 로컬 환경과 배포 환경은 다르다

Live Server 같은 로컬 정적 서버는 HTML/CSS/JS만 보여줄 수 있고, Python 백엔드(`api/summarize.py`)는 실행할 수 없습니다. 그래서 로컬에서 AI 기능을 테스트하면 항상 "네트워크 오류"가 발생하는 것이 정상이며, 이는 버그가 아닙니다. 실제 배포 환경(Vercel)에서만 백엔드까지 포함한 전체 기능이 동작합니다. 이 차이를 이해하지 못하면, 정상적인 코드를 계속 버그로 의심하게 됩니다.

## 8. 디버깅은 "가능성을 하나씩 제거해나가는 과정"

실제로 겪었던 두 가지 버그(폼 태그가 잘못 닫혀 요약 기능이 멈춘 문제, 다크모드 스크립트가 한 페이지에서만 누락된 문제)를 해결한 과정은 아래와 같은 순서를 따랐습니다.

```
증상 확인
  → 브라우저 개발자 도구 Console 탭에서 에러 확인
    → Network 탭에서 실제 요청이 발생하는지 확인
      → 시크릿 모드로 캐시 문제인지 검증
        → 코드를 직접 리뷰하여 원인 확정
```

이렇게 가능성을 하나씩 배제하며 범위를 좁히는 방식은 특정 기술에 국한되지 않는 범용적인 문제 해결 방법입니다.

## 9. 파일/폴더 구조에 담긴 의도

- **HTML 파일**은 사용자가 URL로 직접 접근하는 "진입점"이므로 프로젝트 최상위에 위치합니다. (`.../summarize.html`처럼 짧은 주소 유지)
- **CSS/JS/API**는 HTML이 내부적으로 불러다 쓰는 "부품"이므로 종류별 폴더(`css/`, `js/`, `api/`)로 분리해 관리합니다.

## 10. 환경 변수와 보안의 기본 원칙

- API 키(`ANTHROPIC_API_KEY`)는 코드에 직접 작성하지 않고 환경 변수로 분리해 관리합니다.
- `.env` 파일은 `.gitignore`에 등록하여 Git 커밋 대상에서 제외합니다.
- `git log --all --full-history -- .env` 명령으로 과거 커밋 이력에 민감 파일이 노출된 적이 있는지 직접 검증할 수 있습니다. (본 프로젝트는 검증 결과 노출 이력이 없음을 확인함)

## 11. Git의 기본 흐름과 실수를 통한 학습

- `git add` → `git commit` → `git push` 순서와 각 단계의 역할
- **명령어를 실행하는 위치(현재 디렉토리)가 결과에 영향을 준다**는 것을 실제로 경험함 (하위 폴더에서 `git add .`를 실행해 의도한 파일이 커밋에서 누락된 사례)
- 작업 전후로 `git status`를 확인하는 습관이 실수를 예방한다는 것

---

*이 문서는 Claude(AI 코딩 어시스턴트)와의 대화를 통해 프로젝트를 진행하며 정리한 학습 내용입니다.*

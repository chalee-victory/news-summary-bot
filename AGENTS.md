# 뉴스 요약 봇 작업 지침

## 현재 상태

- 이 저장소는 초기 골격이다. `index.html`, `summarize.html`, `about.html`, `guide.html`과 `api/`, `js/`, `css/`, `images/` 디렉터리가 아직 비어 있을 수 있다.
- 구현이나 의존성을 추가하기 전에 기존 파일과 `README.md`를 확인하고, 현재 구조에 맞는 최소 변경을 선택한다.

## 실행 및 검증

- 정적 페이지를 확인하려면 저장소 루트에서 `py -m http.server 8000`을 실행하고 `http://localhost:8000/`을 연다.
- 현재 프로젝트에 정의된 테스트 명령이나 테스트 의존성은 없다. 새 기능을 추가하면 해당 기능에 맞는 검증 방법과 명령을 함께 문서화한다.
- HTML, CSS, JavaScript를 수정한 뒤 브라우저에서 관련 페이지와 주요 상호작용을 직접 확인한다.

## 구조 규칙

- 페이지 진입점은 루트 HTML 파일에 둔다.
- 브라우저 동작과 API 호출은 `js/`, 스타일은 `css/`, 이미지와 기타 정적 리소스는 `images/`에 둔다.
- 서버리스 또는 백엔드 엔드포인트는 `api/`에 두며, API 키나 비밀값을 클라이언트 코드에 포함하지 않는다.
- 기능을 추가할 때 적절한 페이지, 스크립트, 스타일의 책임을 분리하고 기존 파일을 불필요하게 재구성하지 않는다.

## 환경변수와 보안

- 비밀값은 로컬 `.env`에서만 관리하고 저장소에 커밋하거나 브라우저에 전달하지 않는다.
- `.env`의 자격 증명이 노출되었을 가능성이 있으면 사용을 중지하고 재발급한 뒤, 코드에는 환경변수 이름만 사용한다.
- 외부 API를 연결할 때 입력값 검증, 오류 처리, 요청 실패 시 사용자 피드백을 포함한다.

## 문서화

- 실행 방법, 필요한 환경변수, 테스트 또는 수동 검증 절차가 바뀌면 `README.md`를 갱신한다.
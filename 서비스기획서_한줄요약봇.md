# 서비스 기획서 — 뉴스 한 줄 요약봇 (News Summary Bot)

> AI 웹 개발 과제 제출용 기획서 초안 (v2.0 — URL 크롤링 기반으로 변경)

---

## 1. 서비스 개요

| 항목 | 내용 |
|---|---|
| **서비스명(가안)** | 뉴스 한 줄 요약봇 (News Summary Bot) — 추후 변경 가능 |
| **한 줄 소개** | 뉴스 기사 URL을 붙여넣으면 AI가 본문을 자동으로 가져와 핵심만 3줄로 요약하고 키워드를 뽑아주는 서비스 |
| **목적** | 매번 기사 원문을 처음부터 끝까지 읽지 않아도, 링크 하나로 핵심을 빠르게 파악하게 해준다 |
| **타겟 사용자** | 매일 여러 뉴스를 소비해야 하는 대학생, 직장인 (아침 뉴스 브리핑, 업무용 이슈 트래킹 등) |

---

## 2. 문제 정의 (Why)

- 관심 있는 뉴스가 있어도 기사 전문을 읽을 시간이 부족하다.
- 텍스트를 직접 복사해서 붙여넣는 것조차 번거로운 경우가 많다(특히 모바일 환경).
- 기존 요약 도구는 텍스트 붙여넣기 방식이 대부분이라 "복사 → 붙여넣기"라는 한 단계가 항상 필요했다.
- → **URL만 붙여넣으면 알아서 본문을 가져와 요약**해주는, 한 단계 더 간편한 도구를 제공한다.

---

## 3. 핵심 기능 (What)

### 3-1. 메인 기능: 뉴스 URL 기반 AI 요약
1. 사용자가 뉴스 기사 URL을 입력창에 붙여넣는다.
2. "요약하기" 버튼 클릭 시, 백엔드가 해당 URL에 접속해 **기사 본문을 자동으로 추출(크롤링)**한다.
3. 추출된 본문을 AI에게 전달해 **3줄 요약 + 핵심 키워드 3개**를 생성한다.
4. 결과를 화면에 표시한다.

### 3-2. 부가 기능(선택, 시간 여유 시)
- 원문 링크 바로가기 버튼 (요약 결과 옆에 "원문 보기" 링크 제공)
- 요약 결과 복사하기 버튼
- 다크 모드 (보너스 과제 항목과 연결 가능)

---

## 4. 타겟 사용자 페르소나 (참고용)

| 항목 | 내용 |
|---|---|
| 이름(가상) | 김수연 (23세, 대학생) |
| 상황 | 매일 아침 전공 관련 뉴스를 여러 개 읽어야 하는데 시간이 부족함 |
| 니즈 | 관심 기사 링크를 붙여넣기만 하면 바로 핵심을 확인하고 싶음 |
| 사용 시나리오 | 등굣길 지하철에서 뉴스 앱에서 기사 링크를 복사해 서비스에 붙여넣고, 30초 안에 핵심 파악 후 필요하면 원문으로 이동 |

---

## 5. 페이지/섹션 구성 (최소 3개 이상 요건 충족)

| # | 페이지/섹션명 | 역할 |
|---|---|---|
| 1 | **홈 (Home)** | 서비스 소개, 핵심 가치 제안, "요약하기로 이동" CTA 버튼 |
| 2 | **요약하기 (Summarize)** | 핵심 기능 — URL 입력 → 크롤링 → AI 요약 결과 출력 (실제 AI API 연동 페이지) |
| 3 | **사용법 안내 (How to use)** | 사용 방법 3단계 설명, 크롤링이 잘 안 되는 사이트가 있을 수 있다는 안내 |
| 4 | **소개 (About)** | 서비스 제작 배경, 만든 사람 소개(과제 맥락 설명 가능) |

> 4개 섹션 모두 상단 네비게이션 바에서 이동 가능하도록 구성

---

## 6. AI 기능 상세 설계 (과제 필수 항목)

### 6-1. 입력 (Input)
- **입력 형태**: `<input type="url">` 또는 `<input type="text">` 형태의 URL 입력창
- **입력 제약**:
  - `http://` 또는 `https://`로 시작하는 유효한 URL 형식인지 프론트에서 1차 검증
  - 뉴스 기사 URL을 권장 (일반 웹사이트도 시도는 가능하나 정확도 낮을 수 있음)
- **예시 입력**: `https://news.naver.com/article/...` 등

### 6-2. 처리 (크롤링 + AI 호출) — 2단계로 구성

**1단계 — 본문 크롤링 (신규)**
- 프론트에서 `fetch('/api/summarize', { method: 'POST', body: {url: 사용자입력URL} })` 형태로 백엔드 호출
- 백엔드(`api/summarize.py`)에서:
  1. `requests`로 해당 URL의 HTML을 가져온다
  2. `trafilatura` 라이브러리로 광고/메뉴/댓글을 제외한 **기사 본문만 추출**한다
  3. 본문 추출 실패 시 즉시 에러 응답 반환 (아래 6-4 참고)

**2단계 — AI 요약**
- 추출된 본문 텍스트를 AI API(Claude/OpenAI)에 전달
- 프롬프트 설계(예시):
  > "다음 뉴스 기사 본문을 한국어로 3줄 이내로 요약하고, 핵심 키워드 3개를 뽑아줘. 결과는 JSON 형식으로: {summary: [...], keywords: [...]}"

### 6-3. 출력 (Output)
- **3줄 요약**: 불릿 리스트 형태
- **핵심 키워드 3개**: 태그/칩 UI 형태로 표시
- **원문 링크**: 사용자가 입력한 URL을 "원문 보기" 버튼으로 다시 제공

### 6-4. 실패 처리 (과제 필수 요건 — 최소 1개 이상, URL 방식이라 케이스가 더 다양해짐)

| 실패 케이스 | 원인 | 사용자 안내 메시지(예시) |
|---|---|---|
| **빈 입력** | URL을 입력하지 않음 | "뉴스 기사 URL을 입력해주세요." |
| **URL 형식 오류** | http(s)로 시작하지 않는 등 형식이 잘못됨 | "올바른 URL 형식이 아니에요. (예: https://로 시작)" |
| **크롤링 실패** | 사이트가 접근을 차단(403)하거나 페이지가 존재하지 않음(404) | "해당 페이지에서 기사를 가져올 수 없어요. 다른 기사로 시도해보세요." |
| **본문 추출 실패** | 페이지는 열리지만 기사 본문을 찾지 못함 | "기사 본문을 인식하지 못했어요. 뉴스 기사 URL인지 확인해주세요." |
| **API 오류 (4xx/5xx)** | AI API 호출 실패 | "일시적인 오류가 발생했습니다. 잠시 후 다시 시도해주세요." |
| **응답 지연/타임아웃** | 크롤링 또는 AI 응답이 느림 | "요약을 생성하는 중이에요. 잠시만 기다려주세요..." (로딩 스피너 + 일정 시간 초과 시 안내) |

> 과제 요건은 "최소 1개 이상"이지만, URL 방식으로 바뀌면서 실패 케이스가 자연스럽게 풍부해졌습니다. **빈 입력, URL 형식 오류, 크롤링 실패** 3가지는 구현 난이도가 낮으면서도 실제로 자주 발생할 상황이라 우선순위로 추천합니다.

---

## 7. 기술 스택 (과제 지정 사항 + 크롤링 추가)

| 구분 | 기술 |
|---|---|
| 프론트엔드 | HTML5, CSS3, Vanilla JavaScript (프레임워크 미사용) |
| 백엔드 | Vercel Serverless Functions (Python) |
| 크롤링 | `requests` (HTML 가져오기) + `trafilatura` (본문 추출) |
| AI API | Claude API 또는 OpenAI API (택 1) |
| 배포 | Vercel + GitHub 연동 |
| 버전 관리 | GitHub |

### 예상 프로젝트 구조
```
news-summary-bot/
├── index.html          ← 홈
├── summarize.html       ← 요약하기 페이지 (URL 입력)
├── guide.html            ← 사용법 안내
├── about.html            ← 소개
├── css/
│   └── style.css
├── js/
│   └── main.js           ← fetch 호출, 화면 업데이트, URL 형식 1차 검증
├── api/
│   └── summarize.py     ← 크롤링 + AI API 호출 백엔드 함수
├── requirements.txt     ← requests, trafilatura, (openai 또는 anthropic) 등
├── .env (로컬용, git에는 미포함)
└── README.md
```

### requirements.txt 예상 항목
```
requests
trafilatura
anthropic        # 또는 openai
```

---

## 8. 반응형 디자인 계획

- 데스크톱 / 모바일 최소 2개 화면 크기에서 확인 (과제 요건)
- 브레이크포인트(예시): 768px 기준으로 모바일/데스크톱 레이아웃 분기
- URL 입력창, 버튼, 네비게이션 메뉴가 모바일에서 잘리지 않고 터치하기 쉬운 크기로 조정

---

## 9. 알려진 기술적 리스크 (사전 인지 필요)

- **일부 언론사 사이트는 크롤링을 차단**할 수 있음 (User-Agent 체크, 로봇 배제 표준 등) → 크롤링 실패 시 명확한 안내 메시지로 처리(6-4 참고)
- **자바스크립트 렌더링 기반 사이트**는 서버 사이드 요청만으로 본문을 못 가져올 수 있음 → 데모/시연 시에는 크롤링이 안정적인 사이트(예: 네이버 뉴스 등) 위주로 테스트 권장
- 저작권 이슈: 본문 전체를 사용자에게 그대로 노출하지 않고, **AI가 생성한 요약문만 표시**하므로 서비스 구조상 문제 없음

---

## 10. 차별화 포인트 (템플릿 예시와의 차별화)

- 반려동물 추천이 아닌 **뉴스 요약**이라는 실용적 생산성 도구
- "텍스트 붙여넣기"가 아닌 **"URL만 붙여넣기"**로 한 단계 더 간편한 UX
- 크롤링 실패 케이스까지 세심하게 안내 처리하여 완성도 있는 실패 UX 구현

---

## 11. 진행 현황 (최종 업데이트)

1. ✅ 서비스 기획서 작성 (URL 크롤링 방식으로 업데이트 완료)
2. ✅ 프로젝트 폴더 구조 생성 + GitHub 저장소 생성
3. ✅ 프론트엔드 뼈대 구현 (index.html 등 4개 페이지)
4. ✅ 백엔드 크롤링 로직 구현 (`api/summarize.py`)
5. ✅ 백엔드 AI 연동 (크롤링 결과 → AI 요약)
6. ✅ 프론트-백엔드 연결(`fetch`) 및 실패 처리 UI 구현
7. ✅ 반응형 스타일 적용 및 확인 (데스크톱/모바일)
8. ✅ Vercel 배포 및 실제 URL 테스트
9. ✅ README / 스크린샷 / 증빙자료 정리 및 최종 제출 준비 완료

---

*이 문서는 초안이며, 개발 진행 중 세부 사항(크롤링 라이브러리, 페이지명 등)은 자유롭게 조정 가능합니다.*
